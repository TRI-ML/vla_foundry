# Small VLM for smoke testing. Uses the same transformer architecture as transformer_11m.yaml
# with a tiny ViT encoder. vocab_size is resolved from the data processor's tokenizer at runtime
# (see VLMParams.init_shared_attributes).
type: vlm
transformer:
  <<: !include transformer_11m.yaml
vit: !include vit_tiny.yaml
